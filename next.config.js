/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // trailingSlash ensures consistent URL handling across environments
  trailingSlash: true,
};

module.exports = nextConfig;
